extends ../layout/main

block variables
  - var title = 'Webpack + Pug';
  - var description = 'Description for Webpack + Pug';
  - var pageClass = 'index-page';

block content
  section.section_color_black
    .container.container_grid
      +item('Рядом исторические парки и скверы')
      +item('Полностью обустроенный')
      +item('10 фонтанов на территории')
      +item('6 км велодорожек')
    .container
      h2 Наши квартиры
  //- example-component


block script
  script(type='text/javascript').
    const foo = true;
    let bar = function() {};
    if (foo) {
    bar(1 + 5)
    }